!!!
%html
  %head
    %title
      Sumobot Tournament
    %link{:href => 'http://fonts.googleapis.com/css?family=VT323', :rel => 'stylesheet', :type=>'text/css'}
    = stylesheet_link_tag :all 
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
  .container
    #botimage
      =# image_tag("tsumo#{1+rand(7)}.png", :width => 900)
      = image_tag("tsumo5.png", :width => 900)
    #content
      #navigation
        = link_to "Home", root_path 
        - if can? :manage, :all then
          = link_to "Admin", home_admin_path
        = render "devise/menu/registration_items"
        = render "devise/menu/login_items"
      
      .inner
        = yield
    
  .container#footer
    = link_to image_tag("by-sa.png", :height => 15, :border=>0), 'https://github.com/Hive13/SumoTourney'
    2011 Hive13
